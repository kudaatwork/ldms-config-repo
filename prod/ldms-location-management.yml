# ldms-location-management — production (Spring Cloud Config).
# Docker Compose disabled; MySQL/RabbitMQ/Eureka via env. Same key layout as dev/staging.

server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: ldms-location-management
  docker:
    compose:
      enabled: false
      lifecycle-management: start-only
      start:
        arguments:
          - mysql
          - rabbitmq
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/ldms_location_management?useSSL=true&requireSSL=true&serverTimezone=UTC&connectTimeout=15000&socketTimeout=45000
    username: ${DB_USERNAME:${MYSQL_USER:developer}}
    password: ${DB_PASSWORD:${MYSQL_DEVELOPER_PASSWORD:Password098()!}}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: ${DATASOURCE_POOL_MAX:50}
      minimum-idle: ${DATASOURCE_POOL_MIN:10}
      idle-timeout: ${HIKARI_IDLE_TIMEOUT_MS:600000}
      connection-timeout: ${HIKARI_CONNECTION_TIMEOUT_MS:30000}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: ${JPA_SHOW_SQL:false}
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

management:
  server:
    port: ${MANAGEMENT_SERVER_PORT:9084}
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: ${MANAGEMENT_TRACING_SAMPLING_PROBABILITY:0.05}

logging:
  level:
    root: ${LOGGING_LEVEL_ROOT:WARN}
    projectlx: ${LOGGING_LEVEL_PROJECTLX:INFO}

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration-ms: ${JWT_ACCESS_MS:3600000}
  refresh-token-expiration-ms: ${JWT_REFRESH_MS:86400000}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}
    register-with-eureka: ${EUREKA_REGISTER_WITH_EUREKA:true}
    fetch-registry: ${EUREKA_FETCH_REGISTRY:true}
    eureka-server-connect-timeout-seconds: ${EUREKA_SERVER_CONNECT_TIMEOUT_SECONDS:5}
    eureka-server-read-timeout-seconds: ${EUREKA_SERVER_READ_TIMEOUT_SECONDS:8}
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:true}
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
    instance-id: ${EUREKA_INSTANCE_ID:${spring.application.name}:${server.port}:${random.uuid}}
